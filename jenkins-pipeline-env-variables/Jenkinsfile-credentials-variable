pipeline {
    agent any
    environment {
        JENKINS_CRED_KEY = credentials('JENKINS_SECRET')
    
    }
    stages {
        stage('Print Credentials') {
            steps {
                script {
                    echo "AWS Access Key: ${env.JENKINS_CRED_KEY}"
                }
            }
        }
    }
}
